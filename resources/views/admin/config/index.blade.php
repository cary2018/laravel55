@extends('layouts.menu')@section('content')    <div id="page-wrapper">        <div class="main-page">            <div class="tables">                <div class="bs-example widget-shadow" data-example-id="hoverable-table">                    <h4>                        系统配置:                        <a href="{{url('admin/config/create')}}" class="btn btn-primary">添加配置</a>                    </h4>                    <div class="form-body">                        @if(is_object($errors))                            @if(count($errors)>0)                                @foreach($errors->all() as $item)                                    <h4>{{$item}}</h4>                                @endforeach                            @endif                        @else                            <h4>{{$errors}}</h4>                        @endif                    </div>                    <table class="table table-hover">                        <thead>                        <tr>                            <th>ID</th>                            <th>配置说明</th>                            <th>参数值</th>                            <th>变量名</th>                            <th>排序</th>                            <th>操作</th>                        </tr>                        </thead>                        <tbody>                        <form action="{{url('admin/change')}}" method="post" class="form-horizontal">                            {{csrf_field()}}                            @foreach($data as $v)                                <tr id="del_{{$v->config_id}}" class="inbox-page">                                    <th scope="row">{{$v->config_id}}</th>                                    <td>{{$v->config_tips}}</td>                                    <td>                                        <input type="hidden" name="config_id[]" value="{{$v->config_id}}">                                        {!! $v->_html !!}                                    </td>                                    <td>{{$v->config_name}}</td>                                    <td>{{$v->config_order}}</td>                                    <td>                                        <a href="{{url('admin/config/'.$v->config_id.'/edit')}}">修改</a>                                        /                                        <a href="javascript:;" onclick="delConf({{$v->config_id}})">删除</a>                                    </td>                                </tr>                            @endforeach                            <tr>                                <td colspan="6">                                    <button type="submit" class="btn btn-default">提 交</button>                                </td>                            </tr>                        </form>                        </tbody>                    </table>                </div>                {{$data->links()}}            </div>        </div>    </div>    <script>        function delConf(config_id){            layer.confirm('您确定要删除这篇文章吗？',{                btn:['确定','取消']  //按钮            },function () {                $.post("{{url('admin/config/')}}/"+config_id,{                    '_method':'delete',                    '_token':"{{csrf_token()}}"                },function(data){                    if(data.status==0){                        //location.href = location.href;                        layer.msg(data.msg,{icon:6});                        $("#del_"+config_id).remove();                    }else{                        layer.msg(data.msg,{icon:5});                    }                });            });        }    </script>@endsection