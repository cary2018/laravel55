<!doctype html><html><head>    <meta charset="utf-8">    @yield('info')    <link href="/frontend_file/css/base.css" rel="stylesheet">    <link href="/frontend_file/css/index.css" rel="stylesheet">    <link href="/frontend_file/css/style.css" rel="stylesheet">    <link href="/frontend_file/css/new.css" rel="stylesheet">    <!--[if lt IE 9]>    <script src="/frontend_file/js/modernizr.js"></script>    <![endif]--></head><body><header>    <div id="logo"><a href="{{url('/')}}"><img src="{{url('frontend_file/images/logo.jpg')}}"></a></div>    <nav class="topnav" id="topnav">        @foreach($navs as $v)            <a href="{{$v->nav_url}}"><span>{{$v->nav_name}}</span><span class="en">{{$v->nav_alias}}</span></a>        @endforeach    </nav></header>@section('content')<div class="news">    <h3>        <p>最新<span>文章</span></p>    </h3>    <ul class="rank">        @foreach($new as $v)            <li><a href="{{url('a/'.$v->art_id)}}" title="{{$v->art_title}}" >{{$v->art_title}}</a></li>        @endforeach    </ul>    <h3 class="ph">        <p>点击<span>排行</span></p>    </h3>    <ul class="paih">        @foreach($rhot as $v)            <li><a href="{{url('a/'.$v->art_id)}}" title="{{$v->art_title}}" >{{$v->art_title}}</a></li>        @endforeach    </ul>    <h3 class="links">        <p>友情<span>链接</span></p>    </h3>    <ul class="website">        @foreach($links as $v)            <li><a target="_blank" href="{{$v->link_url}}">{{$v->link_name}}</a></li>        @endforeach    </ul></div>@show<footer>    {!! config('web.web_copyright') !!} <a href="/">网站统计</a></p></footer></body></html>